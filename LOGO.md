# 台标规则

台标从哪来、怎么托管、模块该怎么给、仓库内置的那套怎么维护，都按这里的规则来。节目单的对应规则见 [EPG.md](EPG.md)。

## 规则

1. **不替用户引入别人维护的台标库**：默认不配任何外部台标库（以前默认的 taksssss/tv 是个人维护的仓库，走的 gcore 在大陆 12 个探针里只有 5 个取得到）。台标由我们自己维护：模块给官方图，仓库再内置一套（`logo-pack/`，见下文）。用户想接外部库，自己设 `mexternalLogoBase`（和 `mexternalLogoIndex`），自助的口子一直留着。
2. **来源顺序**：本地上传（后台「我的频道」上传，或把 `<频道名>.png` 放进数据目录 `logos/`）> 频道自己的台标（模块官方 / 咪咕 / m3u 里写的）> 仓库内置台标 > 用户配置的台标库。前面有能用的，后面就不用；频道自己的确认坏了，或还没下载成功（比如海外服务器连不上大陆图床），就用内置的。
3. **本机托管**（`menableLogoCache`，默认开，实现在 `utils/logoCache.js`）：每轮完整更新时，服务端把要用的台标下载到数据目录 `logo-cache/`，按文件头逐张确认是图片，订阅里写本机地址，播放器只找你自己的服务器。
   - 某个来源取不到（4xx）或不是图片，自动换下一个来源；网络出错或还没下载到的，先沿用原地址；全部确认坏了才留空，让播放器显示自己的占位图。
   - 已托管的每周重取一次，重取失败继续用旧图；坏掉的隔天再试；一个月没用到的自动清理。
   - 只代取公网 http(s) 地址，局域网、本机地址一律不碰。
4. **模块要给自己频道的官方台标**：写在频道对象的 `logo` 字段。不要留空指望台标库兜底——默认已经没有台标库了，留空就是没有台标。
   - 取官方来源：官方频道接口下发的图标、官网直播页的频道图标、官方 App 的频道卡。只用官方来源，不引用第三方台标站。
   - 给**能直接访问的图片地址**（http(s)），不要相对路径；官方接口给的是相对路径的，要按官网实际使用的图片域名拼完整，并实际请求确认能取到（河南就踩过：接口给相对路径，拼出来的域名已经 404）。
   - 优先选**频道图标**而不是整台台标或宣传图；同一台多套频道要能区分开。
   - 官方确实没有台标的，留空，并在模块里写明查过哪些地方。
   - 频道表写在代码里、声明了 `catalogVersion` 的模块，改台标要把它加 1：磁盘缓存只有版本变了才会在启动时重建，否则老部署要等下个刷新周期才换上新台标。
5. **景观、慢直播、直播间这类本来就不是电视台名的频道**，留空即可，也不进内置台标。

## 内置台标（logo-pack/）

仓库根目录的 `logo-pack/` 是我们自己维护的一套台标：`<台标名>.png` 加一份 `index.json`（每张的来源、出处、尺寸、哈希，以及「模块/频道编号 → 台标名」的对应表）。随镜像发布，服务端本机直接提供（`/logo-pack/`，实现在 `utils/logoPack.js`），不联网。

- **收什么**：各模块频道的官方台标、咪咕频道台标（打包脚本从一轮完整更新的结果里取），加上官方没有、模块取不到的频道——精选频道这类 m3u 里的台、官方确实没图的台——手工收的图。
- **怎么匹配**：模块频道先按「模块/频道编号」精确对应；其余按台标名匹配，规则和外部台标库一样（原名、去清晰度后的名字、补分组前缀、英文名不分大小写）。「新闻综合」「少儿频道」这类通用名在包里一律带地名存（`内蒙古新闻综合`），外部 m3u 里裸的通用名不会被贴上别家的图。
- **统一格式**：全部转成长边不超过 256px 的 PNG（位图不放大）。
- **维护**（`scripts/build-logo-pack.mjs`）：
  - 刷新官方图：先完整跑一轮更新（本机起一个实例即可），再 `node scripts/build-logo-pack.mjs --data <数据目录>`。同名的一张图，先到先得（央视频的卫视图标先于各省模块）；模块里已经不在的频道不删，内置本来就是兜底备份。
  - 手工收一张：`node scripts/build-logo-pack.mjs --add <台标名> <图片文件或地址> --source <出处页面> --kind official|platform|library`；批量：`--import <目录>`（目录里 `<台标名>.<扩展名>` 加 `sources.json`）；删一张：`--remove <台标名>`。
  - 图四周留白太多加 `--trim`（批量时 `"trim": true`）；官方只有纯白字的加 `--bg <#颜色>` 铺底色，否则浅色界面上整个看不见。
  - 官方图会替换同名的手工图，手工图不会替换官方图。
  - `scripts/test-logo-pack.mjs` 会检查目录和 `index.json` 一一对应、哈希和尺寸对得上，手工图必须写出处和类别。
- **手工图的取图标准**：
  - 按优先级找：电视台官网 / 官方 App / 官方社交账号（kind `official`）> 正规播出平台上该频道的频道图（Samsung TV Plus、Rakuten TV 等，kind `platform`）> 公开台标库（iptv-org、Wikimedia Commons 等，kind `library`，出处写到具体页面）。
  - 必须是频道台标，不要节目海报、宣传图；透明底优先；逐张看过确认没贴错台。
  - 深浅两种界面都要看得清：官方只有纯白字的，先找官方的有底色版本（官方社交账号头像常是），实在没有再铺底色。
- **给别人引用**：地址是 `https://gcore.jsdelivr.net/gh/akiralereal/iptv@main/logo-pack/<台标名>.png`（`raw.githubusercontent.com` 在大陆打不开，jsDelivr 的 gcore 节点可以）。文件名就是对外的约定，已经收进来的台标**不要改名**；清单看 `logo-pack/index.json`。
- **版权**：台标是各电视台的商标，这里只为标识频道。电视台要求撤下的，删掉那张即可。

## 用户自助

- **补单个台标**：后台「我的频道」右上角「🖼 缺台标」列出当前档里所有没图的频道，点进详情上传；或把 `<频道名>.png` 放进数据目录 `logos/`。本地的永远最优先。
- **用公共台标库兜底**：设 `mexternalLogoBase`。用 taksssss/tv 的库就填 `https://gcore.jsdelivr.net/gh/taksssss/tv@main/icon/`，索引会自动带上；其它库需保证 `<基址><频道名>.png` 能访问，并尽量给 `mexternalLogoIndex`（频道名 → 图片的清单），有索引才不会往订阅里写必定 404 的地址。
- **不要托管**：设 `menableLogoCache=false`，订阅里直接写原台标地址，由播放器自己去取。
