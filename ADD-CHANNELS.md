# 新增频道与模块检查清单

以后加抓取模块、往精选频道里加台，台标和节目单都按这份清单走。细则在 [LOGO.md](LOGO.md)（台标）、[EPG.md](EPG.md)（节目单）和 `extractors/registry.js`（模块契约），这里只列顺序和口径。

## 通用口径

- **不默认引入别人维护的源**：台标库、节目单站都不做默认，用户自己配的口子一直留着（`mexternalLogoBase`、后台「EPG 聚合」）。
- **官方优先**：先用电视台自己的官网、官方 App、官方接口；官方平台（央视频、央视网）其次；正规播出平台、公开台标库只在前面都没有时用于台标，并记清出处。
- **用真实请求验证**：图要实际下载、打开看过，节目单要实际取到并抽查播出时间，不凭读代码下结论。
- **不绕过防护**：不绕过登录、会员、DRM、地域限制，也不绕过 App 的加固和证书校验。拿不到就记下来，到此为止。
- **节目单不对外发布**：只给自己的部署用，理由见 EPG.md「不做对外发布」。

## 一、新增抓取模块（extractors/&lt;id&gt;）

### 1. 取流与登记

- 按已有模块的写法：`extractors/<id>/{api,index}.js`（频道表多的拆 `channels.js`），在 `extractors/registry.js` 登记。
- 播放方式：CDN 不看来源头、UA 的用 `relay`；要签名、要特定请求头的用 `proxy`。
- 频道表写在代码里的，声明 `catalogVersion`；以后改频道或台标都要加 1，让老部署启动时重建缓存。

### 2. 台标（LOGO.md 规则 4、5）

按顺序找，找到就停：

1. **频道自己的官方图标**，写在频道的 `logo` 字段：
   - 模块本来就在请求的官方接口里带图标字段的，运行时取；地址稳定的，直接写死在模块里。
   - 必须是完整的 http(s) 地址，实际请求确认：返回 200、确实是图片、小于 2MB、尺寸合理。
   - 优先选频道图标，不用节目海报；深浅两种界面都要看得清。
2. **官方没有频道标，就用所属电视台的台标**，同样取官方的图（先例：广东台经典剧、纪录片、健康）。
3. **还没有，就在模块里留空**，并注释写明查过哪些地方，然后按下文「二」的做法手工收一张进内置库。

- 景观、慢直播、直播间这类不是电视台的频道，留空。
- 做完后刷新内置库备份：本机完整跑一轮更新，再执行 `node scripts/build-logo-pack.mjs --data <数据目录>`。

### 3. 节目单（EPG.md「给新模块接节目单」）

按顺序找，找到就停：

1. **模块自己台的官方接口**，写 `extractors/<id>/epg.js`，挂到模块上（`epg` 字段、`capabilities.epg: true`）。
2. **官方平台有没有收这台**，上星频道尤其要查：
   - 央视频：`capi.yangshipin.cn/api/yspepg/program/<livePid>/<日期>`。
   - 央视网：`api.cntv.cn/epg/getEpgInfoByChannelNew?c=<代号>&serviceId=tvcctv&d=<日期>`。代号没有公开清单，可以按规律逐个试，比如北京台是 btv1 到 btv9。
   - 先例：北京台各频道的节目单取自央视网。
3. **都没有**：在 EPG.md 状态表里记「无官方节目单」，写明查过的地方。不接第三方节目单站。

- 实测：`node scripts/build-epg.mjs <id>`，抽查两三个节目的时间；在别的网络环境下对比时，用 `node scripts/probe-epg.mjs`。
- 更新 EPG.md 状态表（`scripts/test-epg-doc.mjs` 会检查它和代码是否一致）。

### 4. 测试与文档

- `scripts/test-<id>.mjs`；有节目单的再加 `scripts/test-<id>-epg.mjs`。都用按真实响应裁剪的离线夹具，都加进 `package.json` 的测试链。
- `scripts/test-extractors.mjs` 的地区名表里加上这个模块。
- README：「频道覆盖」「内容来源与抓取能力」两处表格、「常见问题 → 台标或节目单缺了一些」里的节目单模块清单，以及「未发布」日志。
- `npm test` 全部通过。

### 5. 实跑验收

用临时数据目录起一个实例（`mdataDir=<临时目录> mport=<空闲端口> node app.js`），跑完一轮完整更新后核对：
- 新模块的频道都在播放列表里；
- 每个台都有台标，且是托管或内置的地址；
- 该有节目单的频道今天有节目。

## 二、往精选频道（IPTV.m3u）加台

- **台标**：不要在 m3u 里写第三方图片地址，收进内置库：
  ```
  node scripts/build-logo-pack.mjs --add <频道名> <图片文件或地址> --source <出处页面> --kind official|platform|library
  ```
  - 频道名与 m3u 里的一字不差。
  - 来源优先级：官方（台网、官方社交账号头像）> 正规播出平台的频道图 > 公开台标库。
  - 只有纯白字的，先找官方的有底色版本；实在没有，加 `--bg <#颜色>` 铺底色。留白太多加 `--trim`。
  - 逐张在深浅两种背景上看过，确认没贴错台。
- **节目单**：精选频道目前没有挂节目单的机制，不接第三方站，缺着就记下来。

## 三、收尾

- 按改动分开提交，信息写清来源和验证方式。
- 发版前核对 README「未发布」日志和提交记录是否对得上。
